{
  "scripts": {
    "update-dependencies": "npm audit &&  ncu --interactive --format group",
    "chrome": "npm run build-chrome && npm run start-chrome",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "build": "webpack --config webpack.config.js",
    "clean-chrome": "rimraf dist/chrome",
    "build-chrome": "npm run clean-chrome && cross-env NODE_ENV=development webpack --config ./webpack/chrome.js",
    "start-chrome": "echo npx web-ext --source-dir=%CD%\\src run -t chromium --start-url file:///%CD%/test/assets/big_buck_bunny.mp4 | cmd",
    "start-firefox": "echo npx web-ext --source-dir=%CD%\\src run -t firefox --start-url file:///%CD%/test/assets/big_buck_bunny.mp4 | cmd"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "css-loader": "^6.7.1",
    "mkdirp": "^1.0.4",
    "rimraf": "^3.0.2",
    "style-loader": "^3.3.1",
    "web-ext": "^7.2.0",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  },
  "devDependencies": {
    "npm-check-updates": "^16.1.0"
  }
}
